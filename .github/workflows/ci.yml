name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install flask flask-cors requests beautifulsoup4 pymupdf

      - name: Check Python syntax
        run: python -m py_compile app.py && echo "app.py syntax OK"

      - name: Check imports
        run: python -c "import flask, fitz, requests, bs4; print('All imports OK')"
